{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'no build step: the ingest service runs TypeScript through tsx'"
  },
  "deploy": {
    "startCommand": "cd server && node --import tsx scripts/migrate.ts && export NODE_ENV=production && exec node --import tsx src/ingest.ts",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
