{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'no build step: the ingest service runs TypeScript through tsx'"
  },
  "deploy": {
    "startCommand": "npm run db:migrate -w server && npm run start:ingest -w server",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
