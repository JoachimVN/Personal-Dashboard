{
  "name": "personal-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "Personal dashboard — life, GitHub, and AI usage at a glance",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run predev -w server && concurrently -n server,client -c blue,magenta \"npm run dev -w server\" \"npm run wait-ready -w server && npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm test -w server",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
